env = Environment()
env.Append(CCFLAGS = ['-g', '-O', '-Wall', '-ansi', '-pedantic', '-Wextra', '-Weffc++', '-fmessage-length=0' ])
env.Append(LIBS = ['GLEW', 'glfw' ,'GL', 'GLU'])
env.Program('mengine', Glob('*.cpp'))
